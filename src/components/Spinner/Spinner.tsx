import './spinner.css';

const Spinner = () => {
  return (
    <img
      src={'../../assets/spinner.gif'}
      className='spinner'
      alt='Loading...'
    />
  );
};

export default Spinner;